import RegisterForm from "../../component/RegisterForm";
import styles from "./scss/RegisterPage.module.scss";

const RegisterPage: React.FC = () => {
  return <RegisterForm itemSize={styles.itemBlock} />;
};

export default RegisterPage;
